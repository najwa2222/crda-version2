<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}}</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Tajawal:wght@300;400;500;700&display=swap');
    body { font-family: 'Tajawal', sans-serif; }
  </style>
</head>
<body class="bg-gray-50">
<div class="container mx-auto p-4 lg:p-8">
  <section class="bg-white rounded-2xl shadow-lg p-6 mx-auto max-w-9xl">
    <div class="rounded-xl border border-gray-200">
      <table class="w-full">
        <thead class="bg-gray-50">
          <tr>
            <th class="px-6 py-4 text-right font-medium text-gray-700">الموضوع</th>
            <th class="px-6 py-4 text-right font-medium text-gray-700">الاسم</th>
            <th class="px-6 py-4 text-right font-medium text-gray-700">اللقب</th>
            <th class="px-6 py-4 text-right font-medium text-gray-700">ب.ت.و</th>
            <th class="px-6 py-4 text-right font-medium text-gray-700">رقم المعاملة</th>
            <th class="px-6 py-4 text-right font-medium text-gray-700">الحالة النهائية</th>
            <th class="px-6 py-4 text-right font-medium text-gray-700">الإجراءات</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-gray-200">
          {{#each services}}
          <tr class="hover:bg-gray-50">
            <td class="px-6 py-4">{{sujet}}</td>
            <td class="px-6 py-4">{{prenom}}</td>
            <td class="px-6 py-4">{{nom}}</td>
            <td class="px-6 py-4">{{cin}}</td>
            <td class="px-6 py-4">{{numero_transaction}}</td>
            <td class="px-6 py-4">
              {{#if statut}}
              <span class="px-4 py-2 rounded-lg {{statusClass statut}}"> 
                {{statut}} 
              </span>
              {{else}}
              <span class="px-4 py-2 rounded-lg bg-yellow-100 text-yellow-800 border-yellow-200" >بصدد الدرس</span>
              {{/if}}
              </span>
            </td>
            <td class="px-6 py-4">
              <div class="flex gap-2">
                <a href="/viewreport?cin={{cin}}&sujet={{sujet}}"
                   class="px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white rounded-lg">
                  عرض تقرير
                </a>
                <a href="/editresult/{{id}}" 
                   class="px-4 py-2 bg-yellow-600 hover:bg-yellow-700 text-white rounded-lg">
                  تعديل
                </a>
              </div>
            </td>
          </tr>
          {{/each}}
        </tbody>
      </table>
    </div>
  </section>
</div>
</body>
</html>